
// Arrays
  // Some and Every Checks
  // Array.prototype.some()                      // is at least one person 19?
  // Array.prototype.every()                       // is everyone 19?
  // Array.prototype.find()                      // Find is like filter, but instead returns just the one you are looking for
  // Array.prototype.findIndex()                         // Find the comment with this ID
  // Array.prototype.filter()                       // 1. Filter the list of inventors for those who were born in the 1500's
  // Array.prototype.map()                        // 2. Give us an array of the inventor first and last names
  // Array.prototype.sort()                       // 3. Sort the inventors by birthdate, oldest to youngest
  // Array.prototype.reduce()                       // 4. How many years did all the inventors live?  // 5. Sort the inventors by years lived